# Tradbot - Production Options Trading System
# Python 3.11+ required

# Core Dependencies
asyncio-contextmanager>=1.0.0
websockets>=11.0.0
aiohttp>=3.8.0
requests>=2.28.0
numpy>=1.24.0
pandas>=1.5.0

# Broker/API
# (Flattrade integration uses REST endpoints configured via environment)

# Utilities
python-dateutil>=2.8.0
pytz>=2023.3
pyyaml>=6.0

# Logging & Monitoring
python-json-logger>=2.0.0

# Optional: For production deployment
# (web server removed; not required for core bot)

# Development (optional) - developer tooling moved to requirements-dev.txt
